def add(a: int, b: int, c: int = 0) -> int:
    """Add two values together.

    Args:
        a: First value.
        b: Second value.
        c: Third value (default is 0).

    Returns:
        Sum of ``a``, ``b``, and ``c``.
    """
    return a + b + c
